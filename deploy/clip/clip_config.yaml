# This file was generated using the `serve build` command on Ray v2.4.0.

importPath: zoo.model.clip.ray_serve:clip

runtimeEnv: '{}'

host: 0.0.0.0

port: 8000

deployments:

- name: clip
  routePrefix: /clip
